## What & why
<!-- One or two lines on the change. Link the issue if there is one. -->

## Changes
<!-- Bullet the concrete changes. -->

## Checklist
- [ ] No unintended changes to frontend templates/layout (e.g. `message.phtml`) or admin assets (e.g. `tradeaze.svg`)
- [ ] Static content / cache impact considered (`setup:static-content:deploy`, `cache:flush`)
- [ ] Tests added or updated where it makes sense (`src/Test/Unit`)
- [ ] `composer` / `phpcs` still pass
- [ ] Backwards compatible, or the breaking change is called out

## Notes for review
<!-- Anything a reviewer should watch for. -->
